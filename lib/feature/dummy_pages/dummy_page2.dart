import 'package:flutter/material.dart';

class DummyPage2 extends StatelessWidget {
  const DummyPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dummy page 2'),
      ),
    );
  }
}
