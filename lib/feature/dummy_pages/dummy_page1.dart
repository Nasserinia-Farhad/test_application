import 'package:flutter/material.dart';

class DummyPage1 extends StatelessWidget {
  const DummyPage1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dummy page 1'),
      ),
    );
  }
}
